import * as React from "react";
/** Opening paragraph — one size up from Body, light weight, grey. One per page maximum. */
export interface LedeProps extends React.HTMLAttributes<HTMLParagraphElement> { children?: React.ReactNode }
export declare function Lede(props: LedeProps): JSX.Element;
