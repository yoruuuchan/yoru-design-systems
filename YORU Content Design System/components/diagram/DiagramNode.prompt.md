A single diagram box. Usually you pass nodes to <Diagram> and never touch this directly.

```jsx
<DiagramNode role="agent" label="Claude Code" meta="sonnet-4.5 · local" status="verified" />
```

Roles: `input` hairline · `process` heavy ink border · `human` rounded + tint · `agent` accent cap + accent tint · `tool` dashed · `data` top double line · `output` filled ink · `external` dashed muted.