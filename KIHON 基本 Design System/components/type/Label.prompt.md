Section label — tracked uppercase caps with an optional right-aligned index. Plain by default.

\`\`\`jsx
<Label index="02/05">Data</Label>
\`\`\`

rule={true} adds a hairline between label and index when a frame needs the division.