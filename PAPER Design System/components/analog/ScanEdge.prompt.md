Adds the darkened edge of a flatbed scan to a plate or spread. Structural, not decorative — it tells you this image was a physical object first.

```jsx
<ScanEdge intensity={0.7}><Plate ratio="3/2" /></ScanEdge>
```

Use on opening images and printed-spread templates. Skip it in Editorial mode.
