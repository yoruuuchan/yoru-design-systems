Lifts one sentence out of the running text. Two per article at most.

```jsx
<PullQuote attribution="M. Sørensen — interview, 2025">
  You cannot photograph the return of the light.
</PullQuote>
```

Quote something that is actually said in the piece, verbatim. `variant="mark"` for interviews, `"rules"` for essays, `"hang"` inside narrow columns.
