Separates sections. Reach for `variant="space"` first — in PAPER whitespace does most of the dividing.

```jsx
<Divider variant="ornament" label="II" />
```

Never stack a divider next to another rule (a MetaBlock or Folio already carries one).
