Closes every page: publication left, running title centre, folio number right.

```jsx
<Folio left="Paper — Issue Nº 04" center="The Slow Return of Winter Light" page="37" />
```

The number is intentionally larger than everything around it. On screen-only pages drop `center` and keep the pair.
