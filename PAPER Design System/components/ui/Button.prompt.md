The only button in PAPER: square corners, sans label, no shadow.

```jsx
<Button variant="solid">Subscribe</Button>
<Button variant="outline" href="/work">View selected work</Button>
```

Never round the corners and never use `accent` more than once per page. Editorial pages usually want `TextLink` instead — a button is for a real transaction.
