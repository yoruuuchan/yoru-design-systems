Inline or standalone navigation link. The default link style in PAPER — prefer it to a Button in running text and at the end of sections.

```jsx
<TextLink href="/work" arrow>View selected work</TextLink>
```

Hover moves the ink to rust; nothing animates or moves.
