Marks which of PAPER's two voices a page, spread or figure is set in.

```jsx
<ModeBadge mode="analog" label />
```

The badge is documentation, not chrome — use it in style guides, colophons and contents pages, not on live editorial pages.
