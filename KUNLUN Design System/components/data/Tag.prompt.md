# Tag

Chip-style tag, optionally removable. Use for filter chips, file tags, comma-replacement metadata.

```jsx
<Tag>SECTOR-A</Tag>
<Tag variant="accent" onRemove={() => removeFilter("relay")}>RELAY-09</Tag>
```
