# CodeBlock

Read-only code surface, line-numbered, with a copy button. No syntax highlight — code is monochrome ink in KUNLUN.

```jsx
<CodeBlock lang="bash" code={`# boot the core
kunlun init --node 07
kunlun status`} />
```
