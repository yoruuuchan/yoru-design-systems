# Card

Lighter container than `Panel` — no header chrome, just optional eyebrow + title. Use for marketing tiles, summary stats, grid items.

```jsx
<Card eyebrow="// CORE-9" title="142.3 MW" interactive>
  Output stable across all nodes.
</Card>
```
