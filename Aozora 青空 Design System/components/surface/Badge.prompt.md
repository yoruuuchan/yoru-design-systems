### Badge

Tiny inline status / count pill. `soft` (default) is the everyday treatment; `solid` is for emphasis; `outline` is the quietest.

```jsx
<Badge>New</Badge>
<Badge tone="success" dot>Online</Badge>
<Badge tone="accent" variant="solid">SUMMER</Badge>
```
