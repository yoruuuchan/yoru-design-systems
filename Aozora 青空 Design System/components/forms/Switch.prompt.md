### Switch

iOS-style toggle. Brand-blue track when on, mid-neutral when off, with a sprung knob.

```jsx
<Switch defaultChecked label="Notifications" />
<Switch checked={on} onChange={setOn} size="sm" />
```
