# Input

Sunken Y2K text field — the inverse-bevel signals "this is where you type." Body serif by default; pass `mono` for filename/ID inputs.

```jsx
<Input label="title" placeholder="untitled room" defaultValue="" />
<Input label="path" mono defaultValue="/mostly_empty/05.jpg" />
```
