# Halo

Radial gradient *behind* a child. Where `Glow` is a hard box-shadow ring, `Halo` is a soft cloud blooming out from the center.

```jsx
<Halo color="peach" size={420}>
  <OverexposedText as="h1" size={120}>core</OverexposedText>
</Halo>
```
