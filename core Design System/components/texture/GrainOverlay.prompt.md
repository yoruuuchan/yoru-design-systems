# GrainOverlay

Absolute grain layer — drop into any `position: relative` parent to give it the JPEG-y feel.

```jsx
<div style={{ position: 'relative' }}>
  <img src="…" />
  <GrainOverlay opacity={0.22} />
</div>
```
