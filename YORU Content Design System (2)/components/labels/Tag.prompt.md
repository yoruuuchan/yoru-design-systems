Topic chips on covers and under titles. Three or fewer per page.

```jsx
<Tag filled>Claude Code</Tag><Tag>MCP</Tag><Tag tone="neutral">实测</Tag>
```

形状随变体走：Signal / Lab 直角，Studio / Special 胶囊——这是四个变体之间唯一的形状差异，不要覆盖。
`filled` 实心版一组里最多一个，用于主话题；其余用描边。`tone="neutral"` 给不属于本栏目的旁支话题。
封面标签控制在三个以内。
