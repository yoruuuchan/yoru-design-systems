Serif pull quote with an accent rule.

```jsx
<Quote cite="Anthropic" source="Claude Code docs">Skills 是给 Agent 的说明书，不是给人的文档。</Quote>
```

两种用法：别人的原话（给 `cite` 和 `source`），或你自己那句最想让人记住的判断（两个都不给）。
宋体、上粗线下发丝线，本身已经是页面上最重的元素，一页最多一处。不要在 Quote 里再打着重号或荧光笔。
