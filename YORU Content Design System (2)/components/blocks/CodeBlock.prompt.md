Dark block for real code. Keep card-page snippets under 12 lines; longer belongs in the WeChat article.

```jsx
<CodeBlock filename=".claude/skills/review.md" lang="md" lines code={"---\nname: review\n---\n只看 diff。"} />
```
Recessed like `PromptBlock`, using the dark pair of stops (`--shadow-inset-dark`): on ink the low stop is black and the high stop is a faint white. These two blocks are the only shadowed things in the system.
