Sets up the page in one sentence. Use at most once per page, directly under the title.

```jsx
<Lede>三个月里我把这套流程跑了四十多遍，这是留下来的部分。</Lede>
```

开篇段，比 `Body` 大一号、字重更轻、颜色更浅。一页最多一段，作用是让读者决定要不要继续读。
两到三句话，别超过四行。它不该总结全文，而该给出继续读下去的理由。
有 `Lede` 的页面通常不需要再放 `Callout`——两个都在会争夺同一个视觉位置。
