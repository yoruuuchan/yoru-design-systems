Wrap any region to add CRT scanlines + soft chromatic grain on top. Use for hero sections, player surfaces, login screens — keep `medium` as default, `strong` for cinematic moments only.

```jsx
<ScanlineOverlay intensity="medium">
  <Hero />
</ScanlineOverlay>
```
