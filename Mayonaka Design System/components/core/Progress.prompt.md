Segmented pixel progress bar. Use for loading, playback position, scrubbers, level meters.

```jsx
<Progress value={42} label="LOADING SIGNAL" />
<Progress value={song.t} max={song.dur} variant="cyan" />
```
