RGB-split glitch headline. Reserve for hero moments, error states, and easter eggs — not body or repeated labels.

```jsx
<GlitchText size={64} as="h1">MIDNIGHT</GlitchText>
```

Pass a short string only (the effect mirrors the children). Pair with `var(--scanlines)` overlay for full CRT.
